grow = false;

x = obj_portal.x;
y = obj_portal.y;


/*
var number = 360 / 80;
var radius = 400;

for (i = 0; i < 360; i += number){

	instance_create_depth(x + lengthdir_x(radius, i), y + lengthdir_y(radius * .75, i), 0, obj_invisible_wall);


}